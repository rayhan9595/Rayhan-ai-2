name: Rayhan AI - Auto Build APK

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master ]

jobs:
  analyze:
    name: Analyze & Lint
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Cache Pub dependencies
        uses: actions/cache@v4
        with:
          path: |
            ${{ env.FLUTTER_HOME }}/.pub-cache
            .dart_tool
          key: pub-${{ hashFiles('**/pubspec.yaml', '**/pubspec.lock') }}
          restore-keys: pub-

      - name: Install dependencies
        working-directory: . # কোড ফোল্ডারের ভেতরে থাকলে ডটের জায়গায় ফোল্ডারের নাম দিন (যেমন: jarvis_project)
        run: flutter pub get

      - name: Run Dart analyzer
        working-directory: . # ডটের জায়গায় আপনার ফোল্ডারের নাম দিন
        run: flutter analyze

  test:
    name: Unit Tests
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: analyze

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Cache Pub dependencies
        uses: actions/cache@v4
        with:
          path: |
            ${{ env.FLUTTER_HOME }}/.pub-cache
            .dart_tool
          key: pub-${{ hashFiles('**/pubspec.yaml', '**/pubspec.lock') }}
          restore-keys: pub-

      - name: Install dependencies
        working-directory: . # ডটের জায়গায় আপনার ফোল্ডারের নাম দিন
        run: flutter pub get

      - name: Run tests
        working-directory: . # ডটের জায়গায় আপনার ফোল্ডারের নাম দিন
        run: flutter test

  build-apk:
    name: Build Release APK
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: test

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Cache Pub dependencies
        uses: actions/cache@v4
        with:
          path: |
            ${{ env.FLUTTER_HOME }}/.pub-cache
            .dart_tool
          key: pub-${{ hashFiles('**/pubspec.yaml', '**/pubspec.lock') }}
          restore-keys: pub-

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
            android/.gradle
          key: gradle-${{ hashFiles('**/android/**/*.gradle*', '**/android/gradle-wrapper.properties') }}
          restore-keys: gradle-

      - name: Install dependencies
        working-directory: . # ডটের জায়গায় আপনার ফোল্ডারের নাম দিন
        run: flutter pub get

      - name: Build release APK
        working-directory: . # ডটের জায়গায় আপনার ফোল্ডারের নাম দিন
        run: flutter build apk --release --split-per-abi

      - name: Upload APK artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Rayhan-AI-release-apk
          path: '**/build/app/outputs/flutter-apk/*.apk'
          if-no-files-found: error
